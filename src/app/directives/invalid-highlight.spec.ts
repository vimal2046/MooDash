import { InvalidHighlight } from './invalid-highlight';

describe('InvalidHighlight', () => {
  it('should create an instance', () => {
    const directive = new InvalidHighlight();
    expect(directive).toBeTruthy();
  });
});
